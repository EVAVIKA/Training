from settings import Settings
if __name__ == "__main__":
    
    Settings.get_instance().add_score_points(10)
    Settings.get_instance().add_score_points(10)
    Settings.get_instance().add_score_points(10)
    Settings.get_instance().add_score_points(10)
    Settings.get_instance().add_score_points(10)
    Settings.get_instance().add_score_points(10)
    Settings.get_instance().add_score_points(10)
    Settings.get_instance().add_score_points(10)
    Settings.get_instance().add_score_points(10)
    Settings.get_instance().say_score()